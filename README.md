
  # Carrossel de parceiros

  This is a code bundle for Carrossel de parceiros. The original project is available at https://www.figma.com/design/gGsLiKgN1KbDnSwud3zZFi/Carrossel-de-parceiros.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  